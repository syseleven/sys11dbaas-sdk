package sys11dbaassdk

const AppName, VersionMajor, VersionMinor, VersionPatch, VersionBuild string = "sys11dbaas-sdk", "0", "0", "1", "dev"
const FullVersion string = AppName + "/" + VersionMajor + "." + VersionMinor + "." + VersionPatch + "-" + VersionBuild
